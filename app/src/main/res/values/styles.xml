<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/pistachio</item>
        <item name="colorPrimaryDark">@color/pistachio_dark</item>
        <item name="colorAccent">@color/mine_shaft</item>

    </style>


    <style name="IconButton">
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:background">@android:color/transparent</item>
        <item name="android:paddingStart">@dimen/list_element_margin</item>
        <item name="android:paddingLeft">@dimen/list_element_margin</item>
        <item name="android:textSize">@dimen/icon_button_size</item>
        <item name="android:tint">@color/iconify_selector</item>
        <item name="android:padding">16dp</item>
        <item name="android:visibility">gone</item>
    </style>

    <style name="AgendaRoomLabel">
        <item name="android:background">@color/white</item>
        <item name="android:textColor">#F50057</item>
        <item name="android:textSize">16sp</item>
        <item name="android:paddingStart">4dp</item>
        <item name="android:paddingEnd">4dp</item>
    </style>

    <style name="AgendaTalkLabel">
        <item name="android:background">@color/white</item>
        <item name="android:padding">4dp</item>
        <item name="android:textSize">14sp</item>
        <item name="android:textColor">#E65100</item>
    </style>

    <style name="SpeakerPicture">

    </style>

</resources>
